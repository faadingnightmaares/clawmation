{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "indicator",
  "description": "The transparent recording-indicator overlay window: it only polls get_status to render the pixel-cat.",
  "windows": ["indicator"],
  "permissions": [
    "core:default"
  ]
}
