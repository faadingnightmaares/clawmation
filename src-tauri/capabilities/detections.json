{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "detections",
  "description": "The fullscreen detection overlay: it only polls get_detections to draw boxes over what the triggers are finding.",
  "windows": ["detections"],
  "permissions": [
    "core:default"
  ]
}
